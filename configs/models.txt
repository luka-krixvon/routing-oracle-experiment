# Operative model pool for run_sequential.sh  —  format: repo_id | quant | tensor_parallel
# Verified on HuggingFace (preflight). Edit freely; one model per line.
# quant: none (fp16) | awq        tensor_parallel: 1 (single 4090) | 2 (both 4090s)
# Peak disk per model with sequential+purge = the largest single download below (~29GB) < 55GB.
#
# ---- open (no login needed) ----
mistralai/Mistral-7B-Instruct-v0.3 | none | 1      # 7.2B fp16 ~15GB VRAM
deepseek-ai/DeepSeek-R1-Distill-Qwen-7B | none | 1 # 7.6B fp16 ~15GB (long-CoT: raise K cost)
Qwen/Qwen2.5-7B-Instruct-AWQ | awq | 1             # 7.6B 4bit ~5.5GB
Qwen/Qwen2.5-14B-Instruct-AWQ | awq | 1            # 14.8B 4bit ~9GB
Qwen/Qwen2.5-32B-Instruct-AWQ | awq | 1            # 32.8B 4bit ~20GB (one 4090, tight)
microsoft/phi-4 | none | 2                         # 14.7B fp16 ~29GB -> needs BOTH 4090s (tp=2)
#   ^ if you prefer one card, swap for a community 4-bit phi-4 (e.g. *-AWQ) and set quant=awq|1
#
# ---- open, DISTINCT lineages (added for pool-composition robustness; apache-2.0, fp16, one card) ----
# These break the Qwen-heavy skew: current pool is 5/8 Qwen lineage (3x Qwen2.5 + DeepSeek-R1-
# Distill-Qwen, arch=qwen2). One model per new family lets 04 report noise_share on FULL /
# one-per-lineage / Qwen-size-sweep pools and a lineage correlation table.
allenai/OLMo-2-1124-7B-Instruct | none | 1         # 7.3B fp16 ~17GB  (AllenAI; fully-open data+code+weights)
01-ai/Yi-1.5-9B-Chat | none | 1                    # 8.8B fp16 ~20GB  (01.AI)
ibm-granite/granite-3.3-8b-instruct | none | 1     # 8.2B fp16 ~19GB  (IBM Granite)
#
# ---- gated: run `huggingface-cli login` AND accept the license on each model page first ----
google/gemma-2-9b-it | none | 1                    # 9.2B fp16 ~18GB  (license:gemma)
meta-llama/Llama-3.1-8B-Instruct | none | 1        # 8.0B fp16 ~16GB  (license:llama3.1)
#
# NOTE: the original gemma-4-12B / gemma-4-26B-A4B are MULTIMODAL (image-text-to-text,
# gemma4_unified) and Llama-3.2-11B-Instruct does NOT exist (3.2@11B is Vision-only) —
# all three removed. Re-add a text-capable substitute if you want those families.
