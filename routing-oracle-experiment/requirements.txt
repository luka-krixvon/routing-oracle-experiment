# Runtime for the routing-oracle experiment (GPU VM).
# vllm pulls a matching torch build; install vllm FIRST on the GPU box.
vllm>=0.6.0
numpy>=1.24
scipy>=1.10
datasets>=2.18
huggingface_hub>=0.23
pyyaml>=6.0
matplotlib>=3.7
statsmodels>=0.14
pandas>=2.0
